import React from 'react';
import { SafeAreaView } from 'react-native';

import Routes from './routes';

const App = () => {
  return (
    <SafeAreaView>
      <Routes />
    </SafeAreaView>
  );
};

export default App;
